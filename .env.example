# Leave this empty to use the local Codex CLI session instead.
OPENAI_API_KEY=

# Optional: force `codex` or `openai`.
AUTOAGENT_BACKEND=codex

# Optional for OpenAI-compatible gateways.
# OPENAI_BASE_URL=
