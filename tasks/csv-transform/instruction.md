You are working inside `/app`.

Read `/app/files/input.csv` and write `/app/output/summary.json`.

The JSON file must contain exactly these keys with numeric values:
- `row_count`
- `total_amount`
- `average_amount`
- `max_amount`
- `min_amount`

Use the data from the CSV file. Do not write explanatory text.
