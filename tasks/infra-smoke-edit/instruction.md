You are working inside `/app`.

Complete these steps:
1. Read `/app/files/input.txt`.
2. Replace the exact token `TODO` with `DONE` in `/app/files/input.txt`.
3. Write `/app/output/result.txt` containing exactly `task completed`.

Do not change the filename or create alternate output locations.
