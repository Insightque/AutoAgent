You are working inside `/app`.

There is an off-by-one bug in `/app/files/calculator.py`.

Fix the bug so that this command passes without changing the tests:

`pytest -q /app/files/test_calculator.py`

Do not modify `/app/files/test_calculator.py`.
